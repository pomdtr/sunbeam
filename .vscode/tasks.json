{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Start Sunbeam",
            "type": "shell",
            "command": [
                "go",
                "run",
                "main.go"
            ],
            "problemMatcher": [],
            "options": {
                "env": {
                    "SUNBEAM_LOG_FILE": "${workspaceFolder}/debug.log"
                }
            }
        }
    ]
}
