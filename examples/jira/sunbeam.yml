name: jira
version: "1.0"
title: Jira
rootItems:
  - title: List Issues
    script: listIssues
    with:
      jql: assignee=currentUser() and status!=Closed
scripts:
  listIssues:
    command: ./jira.sh ${{ jql }}
    mode: filter
    params:
      - name: jql
        type: string
