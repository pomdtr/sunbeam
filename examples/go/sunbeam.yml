name: go-plugin
title: Go Plugin
requirements:
  - go
version: "1.0"
postInstall: |
  go build -o out/plugin
scripts:
  list:
    command: ./out/plugin
    onSuccess: push-page
    page:
      type: list
      mode: filter
